# Profile Registry

## 现在在哪里

- GitHub Profile 以 `Trading OS / Content OS / Agent Product Lab` 为唯一主骨架。
- 主页面只使用 `READY / BUILDING / EXPLORING` 三种状态。
- Profile 面向潜在合作者和同类 builder；完整系统图优先，公开 repo 是下钻入口。
- 私有实现、归档项目、参考项目与长期清单不进入主视图。

## 下一步

- 当任一系统的公开产品状态发生实质变化时，同步更新对应 `Products` 与 `Now / Next`。
- 每月检查一次公开链接、归档状态和 Profile 第一屏的信息密度。
- 新项目只有在公开且具备可用入口与明确核心结果后，才可标记为 `READY`。
